[package]
name = "pam-redface"
version = "0.1.0"
edition = "2024"

[dependencies]
redface-core = { workspace = true }
pam = { package = "pam-bindings", version = "0.3.0" }
uzers = "0.12.2"

[lib]
name = "pam_redface"
path = "src/lib.rs"
crate-type = ["cdylib"]
